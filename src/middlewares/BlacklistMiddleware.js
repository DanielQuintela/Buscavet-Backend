// import BlacklistController from '../controllers/BlacklistController.js';

// export default function checkBlacklist(req, res, next) {
//     const token = req.headers.authorization; // Obtenha o token do cabeçalho da solicitação (ou de onde você o armazena)
//     if (await isTokenBlacklisted(token)) {
//         return res.status(401).json({ message: 'Token inválido' });
//     }
//     next();
// }

// export { checkBlacklist };
